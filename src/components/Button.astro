---
/**
 * Button — primary CTA + secondary + ghost + link variants.
 * Renders as <a> when href is provided, <button> otherwise.
 *
 * Soft UI Evolution treatment per uipro:
 *   - Subtle 200-300ms transitions
 *   - Soft shadows that lift on hover
 *   - Visible focus ring (WCAG AA+)
 */
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost' | 'link' | 'inverse';
  size?: 'sm' | 'md' | 'lg';
  type?: 'button' | 'submit' | 'reset';
  external?: boolean;
  fullWidth?: boolean;
  ariaLabel?: string;
  iconAfter?: boolean;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  type = 'button',
  external = false,
  fullWidth = false,
  ariaLabel,
  iconAfter = false,
} = Astro.props;

const Tag = href ? 'a' : 'button';
const classes = [
  'btn',
  `btn-${variant}`,
  `btn-${size}`,
  fullWidth ? 'btn-full' : '',
  iconAfter ? 'btn-arrow' : '',
].filter(Boolean).join(' ');

const externalAttrs = external
  ? { target: '_blank', rel: 'noopener noreferrer' }
  : {};
---

<Tag
  class={classes}
  href={href}
  type={!href ? type : undefined}
  aria-label={ariaLabel}
  {...externalAttrs}
>
  <span class="btn-label"><slot /></span>
  {iconAfter && (
    <svg
      class="btn-icon"
      aria-hidden="true"
      width="14"
      height="14"
      viewBox="0 0 16 16"
      fill="none"
      stroke="currentColor"
      stroke-width="1.75"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <path d="M2.5 8h11M9.5 4l4 4-4 4" />
    </svg>
  )}
</Tag>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-family: var(--font-body);
    font-weight: 600;
    line-height: 1.2;
    border-radius: var(--radius-pill);
    border: 1px solid transparent;
    cursor: pointer;
    text-decoration: none;
    transition: all var(--duration-normal) var(--ease-soft);
    white-space: nowrap;
    letter-spacing: 0.01em;
    /* A button must never be the reason its container outgrows the screen.
       Without this, a long label's min-content width propagates up through
       grid/flex parents — on /pricing it dragged both arm cards 65px past a
       375px viewport, clipped by the overflow-x guard rather than fixed. */
    max-width: 100%;
  }

  /* Below the phone breakpoint, a two-line button is correct and a card wider
     than the screen is not. Long CTAs ("Book a health restoration consult")
     wrap instead of forcing horizontal overflow. */
  @media (max-width: 30rem) {
    .btn {
      white-space: normal;
      text-wrap: balance;
    }
  }
  .btn:focus-visible {
    outline: none;
    box-shadow: var(--shadow-focus);
  }

  .btn-sm { padding: 0.5rem 1.1rem; font-size: var(--text-sm); }
  .btn-md { padding: 0.75rem 1.5rem; font-size: var(--text-base); }
  .btn-lg { padding: 0.95rem 2rem; font-size: var(--text-base); letter-spacing: 0.015em; }

  .btn-full { width: 100%; }

  .btn-arrow .btn-icon {
    transition: transform var(--duration-fast) var(--ease-ios);
    display: inline-block;
    flex-shrink: 0;
  }
  .btn:hover .btn-icon {
    transform: translateX(3px);
  }
  /* Pressed state: 80ms compression confirms the tap, especially on touch
     where no hover exists. Drops the hover lift while pressed. */
  .btn:active {
    transform: scale(0.985);
    transition-duration: 80ms;
  }

  /* === Primary — TWW Green === */
  .btn-primary {
    background: var(--color-brand-primary);
    color: var(--color-ink-strong);
    box-shadow: var(--shadow-card);
  }
  .btn-primary:hover {
    background: var(--color-brand-primary-dark);
    color: var(--color-paper);
    transform: translateY(-1px);
    box-shadow: var(--shadow-card-hover);
  }

  /* === Secondary — TWW Dark Grey === */
  .btn-secondary {
    background: var(--color-ink-strong);
    color: var(--color-paper);
    box-shadow: var(--shadow-card);
  }
  .btn-secondary:hover {
    background: var(--color-ink);
    color: var(--color-paper);
    transform: translateY(-1px);
    box-shadow: var(--shadow-card-hover);
  }

  /* === Inverse — for dark sections (white-bg button on dark bg) === */
  .btn-inverse {
    background: var(--color-paper);
    color: var(--color-ink-strong);
    box-shadow: var(--shadow-card);
  }
  .btn-inverse:hover {
    background: var(--color-brand-primary);
    color: var(--color-ink-strong);
    transform: translateY(-1px);
  }

  /* === Ghost — outlined === */
  .btn-ghost {
    background: transparent;
    color: var(--color-ink-strong);
    border-color: var(--color-ink-strong);
  }
  .btn-ghost:hover {
    background: var(--color-ink-strong);
    color: var(--color-paper);
  }

  /* === Link — text-style === */
  .btn-link {
    background: transparent;
    color: var(--color-brand-primary-dark);
    padding: 0.25rem 0;
    text-decoration: underline;
    text-underline-offset: 0.22em;
    text-decoration-thickness: 1px;
    border-radius: 0;
    font-weight: 600;
  }
  .btn-link:hover {
    color: var(--color-brand-primary);
    text-decoration-thickness: 2px;
  }
</style>
